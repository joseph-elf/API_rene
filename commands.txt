# single worker
uvicorn main:app --host 127.0.0.1 --port 8000

# 2 workers
gunicorn \
-w 2 \
-k uvicorn.workers.UvicornWorker \
main:app \
--bind 127.0.0.1:8000 \
--access-logfile - \
--error-logfile -